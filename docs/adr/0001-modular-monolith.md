# ADR-0001：MVP 采用模块化单体

- 状态：已接受
- 日期：2026-07-14

## 背景

PRD 将系统描述为多个微服务，但初期团队规模小，核心领域模型仍在快速调整。过早拆分会增加部署、调用链、事务一致性和故障排查成本。

## 决策

核心业务采用 Go 模块化单体，并通过领域模块隔离代码和数据访问。Data Agent、算法引擎、Tracker 与 MCP Server 保持独立服务。

## 约束

- 模块之间不能直接访问彼此内部表结构。
- 跨模块调用必须通过公开 Service 接口。
- Outbox 事件作为未来拆分服务的边界。
